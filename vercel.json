{
   "projects": [
      {
         "name": "client",
         "root": "client",
         "buildCommand": "npm run build",
         "outputDirectory": "dist"
      },
      {
         "name": "server",
         "root": "server"
      }
   ]
}
